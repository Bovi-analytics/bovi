name: Setup Python with uv
description: Install uv and the requested Python version.

inputs:
  python-version:
    description: Python version to install.
    required: true

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v8
    - shell: bash
      run: uv python install "${{ inputs.python-version }}"
