name: Install Azure Functions Core Tools
description: Install Azure Functions Core Tools v4.

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        set -euo pipefail
        sudo apt-get update
        sudo apt-get install -y azure-functions-core-tools-4
