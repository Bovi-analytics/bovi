name: bovi
description: Azure infrastructure for the Bovi dairy analytics platform
runtime:
  name: python
  options:
    toolchain: uv
    virtualenv: .venv
    program: src
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
